#pragma once
#include <string>

class GenerationProperties
{
  public:
    static const std::string GENERATION_PROPERTIES_ITEM_TYPE;
    static const std::string GENERATION_PROPERTIES_VALUE;

  protected:
    GenerationProperties();
    ~GenerationProperties();
};

