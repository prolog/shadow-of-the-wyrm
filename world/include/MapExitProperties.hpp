#pragma once
#include <string>

class MapExitProperties
{
  public:
    static const std::string MAP_EXIT_PROPERTIES_DEPTH;
    static const std::string MAP_EXIT_PROPERTIES_MAX_DEPTH;
    static const std::string MAP_EXIT_PROPERTIES_INCREMENT;

  protected:
    MapExitProperties();
    ~MapExitProperties();
};

