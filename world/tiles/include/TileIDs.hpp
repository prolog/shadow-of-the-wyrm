#pragma once
#include <string>

class TileMapGeneratorIDs
{
  public:
    static const std::string MAP_GENERATOR_ID_ISEN_DUN;
    static const std::string MAP_GENERATOR_ID_LAST_ISLAND_LAIR;

  protected:
    TileMapGeneratorIDs();
    ~TileMapGeneratorIDs();
};

