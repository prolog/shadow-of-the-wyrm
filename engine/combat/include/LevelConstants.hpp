#pragma once
#include "common.hpp"

class LevelConstants
{
  public:
    static const uint DEFAULT_HIT_DICE;
    static const uint DEFAULT_AP_DICE;

  protected:
    LevelConstants();
    ~LevelConstants();
};
