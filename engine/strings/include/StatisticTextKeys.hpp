#pragma once
#include <string>

class StatisticTextKeys
{
  public:
    static const std::string STATISTIC_GAIN_SPEED;
    static const std::string STATISTIC_LOSE_SPEED;

  protected:
    StatisticTextKeys();
    ~StatisticTextKeys();
};

