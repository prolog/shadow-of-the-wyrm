#include "APRegenerationCalculator.hpp"

uint APRegenerationCalculator::calculate_minutes_per_arcana_point(CreaturePtr creature)
{
  uint minutes_per_arcana_point = 40; // FIXME

  if (creature)
  {
  }

  return minutes_per_arcana_point;
}
