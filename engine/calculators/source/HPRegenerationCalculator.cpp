#include "HPRegenerationCalculator.hpp"

uint HPRegenerationCalculator::calculate_minutes_per_hit_point(CreaturePtr creature)
{
  uint minutes_per_hit_point = 30; // FIXME

  if (creature)
  {
  }

  return minutes_per_hit_point;
}
