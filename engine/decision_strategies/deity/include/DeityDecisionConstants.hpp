#pragma once

class DeityDecisionConstants
{
  public:
    static const float HEAL_HP_MAX_PCT;
    static const float HEAL_AP_MAX_PCT;
    static const float PROTECT_SOAK_PER_LEVEL;
    static const float PROTECT_EVADE_PER_LEVEL;
    static const float PROTECT_RESISTANCE_MODIFIER;

  protected:
    DeityDecisionConstants();
    ~DeityDecisionConstants();
};
