#pragma once
#include <string>

class HelpCommandKeys
{
  public:
    static const std::string KEYBINDINGS;
    static const std::string EXIT_HELP;

  protected:
    HelpCommandKeys();
    ~HelpCommandKeys();
};
